import React from 'react';
import Button from '../../Componentes/SharedComponents/Button/Button';

const HomePage = () => {
    return (
        <div>
        </div>
    );
};

export default HomePage;